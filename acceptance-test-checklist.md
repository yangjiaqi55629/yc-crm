# 元川 AI 轻量 CRM 验收清单与测试样例

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v1.0 |
| 适用版本 | 当前 CRM 首期版本 |
| 测试对象 | `crm/` 独立 Web 系统及门户留资接收接口 |
| 测试依据 | `requirements.md`、`technical-design.md`、当前代码实现 |
| 执行角色 | CRM 管理员/销售、门户接口联调人员 |

## 1. 使用说明与通过标准

### 1.1 执行前准备

1. 使用 Node.js 22 启动 CRM，并访问 `http://localhost:3000/login`。
2. 测试环境设置独立数据库，不使用真实客户信息；测试完成后可清理该测试数据库。
3. 准备管理员账号；开发环境默认可使用 `admin / ChangeMe123!`。生产环境必须设置 `CRM_ADMIN_PASSWORD`。
4. 门户接口验收时，设置 `PORTAL_SYNC_SECRET`，并使用正确的 HMAC-SHA256 请求签名。
5. 每个用例执行后，在“结果”列记录：**通过 / 不通过 / 阻塞**，并附截图或接口响应证据。

### 1.2 建议测试数据

| 编号 | 姓名 | 手机号 | 用途 |
| --- | --- | --- | --- |
| D-01 | 王小明 | `13800138001` | 手动新建、编辑、归档 |
| D-02 | 李明 | `13800138002` | 跟进、状态与提醒 |
| D-03 | 张敏 | `+86 138-0013-8003` | 手机号规范化与重复留资 |
| D-04 | 陈晨 | `13800138004` | 今日应跟进 |
| D-05 | 周琪 | `13800138005` | 已逾期 |

### 1.3 准入规则

- 所有 P0 且标记为“可执行”的用例必须通过。
- 标记为“条件验收”的项目，需要满足其列出的外部条件后才能判定。
- 标记为“当前缺口”的项目不得填写为通过；应列入上线前待办或经产品负责人书面降级确认。
- 不允许使用真实手机号、邮箱或生产数据作为本地测试数据。

## 2. 当前版本验收范围结论

| 范围 | 当前状态 | 验收处理 |
| --- | --- | --- |
| 登录、客户管理、跟进、CRM 内提醒、门户接收端、时间线 | 已实现 | 按本清单执行功能验收。 |
| “保持当前状态”保存跟进 | 已修复 | 必须执行 `FU-05` 回归用例。 |
| 门户主动推送、Outbox、失败自动重试 | 已在同级 `../portal` 本地源码实现，尚未部署到真实门户服务器 | 完成部署与环境变量配置后执行 `SYNC-07`、`SYNC-08`。 |
| 同步异常的门户状态查询与“重新推送” | 当前缺口：现页面仅展示 CRM 接收端异常并支持标记已处理 | 不可验收为“自动/人工重试已完成”。 |
| 按标签筛选客户 | 当前缺口：客户列表已展示标签，但没有标签筛选控件 | `CUS-08` 应记录为不通过，需补开发。 |
| 真实 AI 模型服务 | 已接入 Agnes `agnes-2.5-flash`，并完成 CRM 端到端调用验证 | 已验证正常响应；仍需按 `AI-04` 演练超时和异常返回。 |
| HTTPS、备份、正式部署 | 条件验收：本地开发环境不覆盖 | 生产发布前执行 `OPS-01` 至 `OPS-03`。 |

## 3. 功能验收用例

### 3.1 登录与会话

| ID | 优先级 | 测试步骤 | 预期结果 | 结果 |
| --- | --- | --- | --- | --- |
| AUTH-01 | P0 | 访问登录页，输入正确账号和密码并登录。 | 进入工作台；浏览器获得受保护会话；可查看客户数据。 | 待执行 |
| AUTH-02 | P0 | 输入错误密码登录。 | 停留在登录页，展示明确错误提示；不得进入工作台。 | 待执行 |
| AUTH-03 | P0 | 清除站点 Cookie 后，直接访问 `/customers`、某客户详情、`/sync-errors`。 | 均跳转或拒绝访问至登录页；不得泄露业务数据。 | 待执行 |
| AUTH-04 | P0 | 登录后点击退出，再访问任一业务页。 | 会话失效，必须重新登录。 | 待执行 |

### 3.2 客户档案、检索与归档

| ID | 优先级 | 测试步骤 | 预期结果 | 结果 |
| --- | --- | --- | --- | --- |
| CUS-01 | P0 | 新建客户 D-01；仅填写姓名、手机号，来源选“手动录入”。 | 创建成功，默认状态为“待跟进”，来源正确，客户详情出现“手动创建客户”时间线。 | 待执行 |
| CUS-02 | P0 | 为 D-01 填写邮箱、公司、职位、地区、行业、需求描述、预算范围和两个标签后保存。 | 详情页与列表显示最新资料及标签；时间线出现资料更新事件。 | 待执行 |
| CUS-03 | P0 | 再次手动新建手机号与 D-01 相同的客户。 | 系统拒绝创建重复客户并提示手机号重复；客户列表仍只有一名该手机号客户。 | 待执行 |
| CUS-04 | P0 | 分别以姓名片段、完整手机号、邮箱片段搜索 D-01。 | 每种搜索均返回正确客户；无匹配条件时展示空状态。 | 待执行 |
| CUS-05 | P0 | 准备门户来源与手动来源、不同状态、今日/逾期/待跟进客户；分别单独和组合筛选状态、来源、待办。 | 每个筛选结果准确；组合条件只返回同时满足条件的客户。 | 待执行 |
| CUS-06 | P1 | 编辑 D-01 的状态、标签和资料后刷新页面。 | 修改持久化；状态变更出现在时间线；标签可新增、删除并正确展示。 | 待执行 |
| CUS-07 | P0 | 归档 D-01，检查常规客户列表、详情和历史；再执行恢复。 | 归档客户从常规列表隐藏，历史不丢失；恢复后重新出现在列表；两个动作均写入时间线。 | 待执行 |
| CUS-08 | P0 | 为不同客户设置不同标签，尝试仅筛选某一标签。 | **当前缺口**：客户列表无标签筛选入口，不能满足 PRD 的“按标签筛选”；记录为不通过。 | 当前缺口 |

### 3.3 跟进记录、状态与时间线

| ID | 优先级 | 测试步骤 | 预期结果 | 结果 |
| --- | --- | --- | --- | --- |
| FU-01 | P0 | 打开 D-02，新增一条跟进：沟通方式“电话”、填写沟通内容、结果、跟进时间。 | 保存成功；新增记录立即出现在时间线；“最近跟进”更新为本次时间。 | 待执行 |
| FU-02 | P0 | 新增跟进时把状态从“待跟进”改为“跟进中”。 | 客户状态更新；时间线同时保留跟进记录与状态变更事件。 | 待执行 |
| FU-03 | P0 | 新增跟进并设置下次跟进时间为今天稍后的时间。 | 客户详情显示下次跟进时间，客户进入“今日应跟进”队列。 | 待执行 |
| FU-04 | P0 | 连续新增两条不同时间的跟进。 | 两条历史都保留；时间线按发生时间倒序展示；第二条的下次跟进计划替换旧计划。 | 待执行 |
| FU-05 | P0 | 在“更新状态”保持默认的“保持当前状态”，填写必填沟通内容并保存。 | 保存成功；页面不再显示 `Invalid option` 报错；客户原有状态不变。 | 待执行 |
| FU-06 | P0 | 不填“沟通内容”直接保存；再输入超过约定长度的内容。 | 前端或接口阻止保存，并给出可理解的校验提示；不得写入半条跟进记录。 | 待执行 |

### 3.4 CRM 内提醒与工作台

| ID | 优先级 | 测试步骤 | 预期结果 | 结果 |
| --- | --- | --- | --- | --- |
| REM-01 | P0 | 对 D-04 设置今天任意时刻的下次跟进时间。 | 工作台“今日应跟进”数量与列表均包含 D-04；条目含姓名、手机号、状态、下次跟进时间。 | 待执行 |
| REM-02 | P0 | 对 D-05 设置早于当前时间的下次跟进时间。 | 工作台“已逾期”数量与列表均包含 D-05；客户详情以逾期样式提示。 | 待执行 |
| REM-03 | P0 | 新建一名状态为“待跟进”、但不设置下次跟进时间的客户。 | 该客户可通过客户列表“待跟进”筛选找到。 | 待执行 |
| REM-04 | P0 | 对已逾期客户新增一次跟进，并设置新的未来下次跟进时间。 | 客户从逾期队列移除，进入对应的新待办队列；旧待办不重复保留。 | 待执行 |
| DASH-01 | P0 | 对照客户列表筛选结果，检查工作台的今日新增、待跟进、今日应跟进、已逾期、高意向数量。 | 各指标与同一数据库条件下的列表结果一致；重复门户留资不重复计入“今日新增客户”。 | 待执行 |
| DASH-02 | P0 | 点击工作台中今日待跟进、逾期、近期新增、高意向客户条目。 | 每个条目均跳转到正确的客户详情。 | 待执行 |

### 3.5 门户留资接收、去重与安全

> 以下用例测试 CRM 接收端。请求体应包含 `eventKey`、`portalLeadId`、`name`、`phone`、`email`、`source`、`submittedAt`。在生产模式，必须计算 `x-portal-timestamp` 与 `x-portal-signature`；签名原文为 `${timestamp}.${rawJsonBody}`。

| ID | 优先级 | 测试步骤 | 预期结果 | 结果 |
| --- | --- | --- | --- | --- |
| SYNC-01 | P0 | 向 `POST /api/integrations/portal/leads` 发送 D-03 的合法、带正确签名的新事件 `uat-event-0001`。 | 返回 `201`；创建一名来源“门户网站”、状态“待跟进”的客户；产生留资历史与时间线。 | 条件验收 |
| SYNC-02 | P0 | 使用相同请求体和相同 `eventKey` 再发送一次。 | 返回 `200`；不新增客户、不新增第二条留资历史或时间线；响应表明为重复事件。 | 条件验收 |
| SYNC-03 | P0 | 使用新的 `eventKey`，但手机号改为 D-03 的另一种格式（如 `13800138003`）发送。 | 不新建第二名客户；客户详情新增一条“门户再次留资”历史和时间线。 | 条件验收 |
| SYNC-04 | P0 | 先在 CRM 手动补充 D-03 的公司、需求、标签和状态；再用相同手机号发送门户事件，携带不同姓名或邮箱。 | CRM 手动维护的公司、需求、标签、状态不被覆盖；原始姓名、手机号、邮箱在门户留资历史与时间线中保留。 | 条件验收 |
| SYNC-05 | P0 | 使用错误签名、缺少签名、过期时间戳各发送一次。 | 均返回 `401`；不创建客户、不写入门户留资事件。 | 条件验收 |
| SYNC-06 | P0 | 分别发送缺少姓名、手机号过短、`eventKey` 少于 8 位、邮箱格式错误的请求。 | 返回 `400`；不创建客户、不写入留资事件。 | 条件验收 |
| SYNC-07 | P0 | 将本地验证通过的门户版本部署到服务器并配置 CRM 接收地址、共享密钥；在真实门户填写一条全新留资，计时观察 CRM。 | 门户与 Outbox 在同一事务写入，并立即签名推送到 CRM；正常情况下 CRM 在 60 秒内出现客户或留资事件。 | 条件验收 |
| SYNC-08 | P0 | CRM 暂停服务后在门户留资，恢复 CRM 后观察自动重试；同时调用门户重试状态接口核对次数与状态。 | 失败任务留在 `crm_sync_outbox`，按指数退避自动重试；恢复后只创建一个客户和一条对应留资事件。 | 条件验收 |
| SYNC-09 | P0 | 在 CRM“同步异常”页查看门户失败事件，并点击“重试”。 | **当前缺口**：当前页面只能查看 CRM 接收端异常并标记已处理，不能触发门户重新推送。 | 当前缺口 |

### 3.6 同步异常页面

| ID | 优先级 | 测试步骤 | 预期结果 | 结果 |
| --- | --- | --- | --- | --- |
| ERR-01 | P0 | 在可控测试环境构造一次 CRM 接收端入库异常后，打开“同步异常”页面。 | 可看到异常原因、事件键（如可解析）、产生时间和待处理状态。 | 条件验收 |
| ERR-02 | P1 | 点击一条 CRM 接收端异常的“标记已处理”。 | 状态改为已处理，并记录处理时间；不伪造“已重新推送成功”。 | 条件验收 |
| ERR-03 | P0 | 验证异常页面没有泄露完整客户资料、签名密钥或请求原文。 | 页面仅展示定位所需摘要，敏感信息不直接暴露。 | 待执行 |

### 3.7 AI 销售建议

| ID | 优先级 | 测试步骤 | 预期结果 | 结果 |
| --- | --- | --- | --- | --- |
| AI-01 | P0 | 打开包含需求与跟进记录的客户详情，点击“生成销售建议”。 | 页面展示客户画像、需求/意向、沟通重点、建议问题、下一步行动和话术；同时写入时间线。 | 待执行 |
| AI-02 | P0 | 点击“复制话术”，粘贴到文本编辑器。 | 粘贴内容与页面话术一致；AI 不自动修改客户状态、跟进内容或下次跟进时间。 | 待执行 |
| AI-03 | P0 | 在未配置 AI 服务时生成建议。 | 页面明确标注为“本地规则建议”；客户资料不发送至外部模型；分析结果可留存并在刷新后查看。 | 待执行 |
| AI-04 | P1 | 已配置 Agnes 服务地址、密钥和 `agnes-2.5-flash`；分别测试正常响应、超时、异常输出。 | 正常响应保存结构化 AI 建议并显示模型名；超时、非 2xx 或不完整输出展示错误且不写入半成品建议。 | 部分通过 |

### 3.8 数据完整性、边界与运维

| ID | 优先级 | 测试步骤 | 预期结果 | 结果 |
| --- | --- | --- | --- | --- |
| DATA-01 | P0 | 使用 `138 0013 8003`、`+86 138-0013-8003`、`0086 13800138003` 分别创建/同步。 | 系统识别为同一规范化手机号，不产生重复客户。 | 待执行 |
| DATA-02 | P0 | 在保存跟进、修改状态、设置下次跟进后刷新页面并重新登录。 | 客户状态、最近/下次跟进和时间线均一致；不出现只更新部分字段的半完成数据。 | 待执行 |
| DATA-03 | P0 | 观察 Git 仓库、浏览器页面和错误提示。 | 不包含 `.env.local`、CRM SQLite 数据库、AI 密钥、门户同步密钥或完整会话令牌。 | 待执行 |
| OPS-01 | P0 | 生产环境设置 `CRM_ADMIN_PASSWORD`、`PORTAL_SYNC_SECRET`，使用 HTTPS 启动。 | 未配置生产密码时服务拒绝使用默认账号；有配置时可正常登录；门户请求仅经签名校验接收。 | 条件验收 |
| OPS-02 | P0 | 备份 CRM 数据库，创建测试数据后从备份恢复到隔离环境。 | 可恢复客户、跟进、时间线、标签、留资和 AI 历史；恢复验证过程有记录。 | 当前缺口 |
| OPS-03 | P1 | 重启 CRM 服务后访问登录、列表、详情和数据库。 | 服务自动迁移数据库后正常运行；既有数据可读写，无损坏。 | 条件验收 |

## 4. 端到端业务验收场景

### E2E-01：单人销售从留资到跟进

1. 使用 `SYNC-01` 创建一名门户客户。
2. 登录工作台，确认“今日新增”增加且能进入该客户详情。
3. 为客户补充公司、需求、预算和标签。
4. 新增电话跟进，将状态设为“跟进中”，并设置明天的下次跟进。
5. 生成并复制销售建议。

**验收预期：** 客户只有一个档案；门户留资、资料更新、跟进、状态变更和 AI 生成均进入时间线；客户出现在对应待办队列；AI 只提供建议，不自动写业务字段。

### E2E-02：客户再次留资不重复

1. 对 E2E-01 的客户用不同 `eventKey` 再次发送同手机号门户事件。
2. 检查客户列表数量、客户详情的门户留资历史与时间线。
3. 检查销售手动维护的标签、需求、状态和跟进计划。

**验收预期：** 不新增第二个客户；新增一条原始留资历史和“门户再次留资”事件；人工维护的数据不被覆盖。

### E2E-03：逾期客户重新安排

1. 为客户设置已过去的下次跟进时间。
2. 在工作台确认其进入逾期清单。
3. 完成一次跟进，设置新的未来时间。
4. 刷新工作台与客户列表。

**验收预期：** 客户从逾期清单移除，仅在新的时间对应的待办中出现；跟进历史完整保留。

## 5. 验收记录与签署

| 验收项 | 结论 | 证据链接/截图 | 验收人 | 日期 |
| --- | --- | --- | --- | --- |
| P0 可执行用例 |  |  |  |  |
| 条件验收用例 |  |  |  |  |
| 当前缺口处理决定 |  |  |  |  |
| 整体验收结论 | 通过 / 有条件通过 / 不通过 |  |  |  |

### 上线判定建议

- **本地 CRM 演示版**：完成所有“可执行”的 P0 用例后，可判定为通过。
- **可承接真实门户留资的正式版**：还必须部署门户 Outbox、配置真实推送密钥，完成 CRM 侧重试入口和真实联调用例 `SYNC-07` 至 `SYNC-09`。
- **满足 PRD 全量范围的正式版**：还必须补齐按标签筛选，以及生产 HTTPS、备份与恢复验证。
